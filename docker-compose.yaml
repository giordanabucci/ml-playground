services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "18000:18000"
    environment:
      - PYTHONUNBUFFERED=1

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - api